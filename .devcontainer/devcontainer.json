{
  "name": "DAW_DAM_DEV",
  "containerName": "DAW_DAM_DEV",
  "secrets": {"VERSION":{"description": "0.2-Beta"}},
  "dockerComposeFile": "docker-compose.yml",
  "service": "java",
  "workspaceFolder": "/workspace",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "vscjava.vscode-java-pack",
    "ms-python.python",
    "felixfbecker.php-debug",
    "bmewburn.vscode-intelephense-client",
    "ms-azuretools.vscode-docker",
    "esbenp.prettier-vscode",
    "ms-vscode.live-server"
  ],
  "forwardPorts": [80, 3306, 8080],
  "remoteUser": "root",
  "shutdownAction": "stopCompose",
  "postStartCommand": "git clone --quiet https://github.com/caspero94/DAW.git /tmp/DAW && rsync -aq --exclude='.git' --exclude='.devcontainer/.gitignore' /tmp/DAW/ /workspace/ && rm -rf /tmp/DAW && git clone --quiet https://github.com/caspero94/BIBLIOTECA.git /tmp/BIBLIOTECA && mkdir -p /workspace/Biblioteca && rsync -aq --exclude='.git' /tmp/BIBLIOTECA/ /workspace/Biblioteca/ && rm -rf /tmp/BIBLIOTECA"
}
